import React from 'react';
import { Text, View } from 'react-native';

class Profile extends React.Component {
    render() {
      return (
          <View><Text>{profile.name}  .  {profile.count}</Text></View>
        )
    }
}

export default Profile;
